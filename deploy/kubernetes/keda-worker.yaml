apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-platform-worker
  namespace: agent-platform
spec:
  scaleTargetRef:
    name: agent-platform-worker
  pollingInterval: 5
  cooldownPeriod: 60
  minReplicaCount: 2
  maxReplicaCount: 50
  triggers:
    - type: postgresql
      metadata:
        connectionFromEnv: AGENT_PLATFORM_DATABASE_URL
        query: >-
          SELECT COUNT(*) FROM execution_jobs
          WHERE state IN ('READY', 'RETRY') AND available_at <= NOW()
        targetQueryValue: "1"
